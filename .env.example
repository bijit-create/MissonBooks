# Comma-separated pool of Gemini API keys.
# Requests round-robin across keys and auto-failover to the next key on
# 429 / quota / invalid-key errors.
GEMINI_API_KEYS=key1,key2

# Optional: single-key fallback. Used only if GEMINI_API_KEYS is unset.
# GEMINI_API_KEY=your_single_key
